version: '2'
services:
  agent:
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      AGENT_PAT:
      AGENT_POOL:
      AGENT_URL:
    stdin_open: true
    tty: true
    restart: always
